import { QueryProvider } from '@/app/providers/QueryProvider'

function App() {
  return (
    <QueryProvider>
      <main>
        <h1>Weather Forecast</h1>
      </main>
    </QueryProvider>
  )
}

export default App
